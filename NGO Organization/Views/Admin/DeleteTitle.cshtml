@model NGO_Organization.Models.tbl_TeamMemberTitles

@{
    ViewBag.Title = "DeleteTitle";
}


<h2>Delete Title</h2>

<style>
    body > main > div > a {
        font-size: 16px;
        padding: 10px 26px;
        border-radius: 6px;
        font-weight: bold;
        background: cadetblue;
        color: white;
        font-weight: bold;
    }

        body > main > div > a:hover {
            color: white;
        }
</style>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.TitleID)

        <div class="form-group">
          
            <div class="col-md-10">
                @Html.HiddenFor(model => model.TitleName, new { htmlattributes = new { @class = "form-control" } })
                @*@Html.ValidationMessageFor(model => model.TitleName, "", new { @class = "text-danger" })*@
            </div>
        </div>

        <div class="form-group">
           
            <div class="col-md-10">
                @Html.HiddenFor(model => model.AdminID, new { htmlAttributes = new { @class = "form-control" } })
                @*@Html.ValidationMessageFor(model => model.AdminID, "", new { @class = "text-danger" })*@
            </div>
        </div>

        <div class="form-group">
            Product Status
            <div class="col-md-10">
                @Html.EditorFor(model => model.CategoryStatus, new { htmlAttributes = new { @class = "form-control", @Value = 0, @readonly = "Readonly", Style = "padding: 10px;font-size : 18px;" } })
                @*@Html.ValidationMessageFor(model => model.CategoryStatus, "", new { @class = "text-danger" })*@
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Delete" class="btn bg-gradient-primary mt-3" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "TeamMemberTitle", "Admin", htmlAttributes: new { @class = "btn bg-gradient-primary " })
</div>
